{
"items": [
{{#each entries}}
    {
    "id": "{{id}}",
    "createdAt": "{{createdAt}}",
    "userIdCode": "{{userIdCode}}",
    "ipAddress": "{{ipAddress}}",
    "action": "{{action}}",
    "userAgent": "{{userAgent}}",
    "firstName": "{{firstName}}",
    "lastName": "{{lastName}}",
    "clientName": "{{clientName}}"
    }{{#unless @last}},{{/unless}}
{{/each}}
],
"page": {{page}},
"pageSize": {{pageSize}},
"totalPages": {{#if entries.[0]}}{{entries.[0].totalPages}}{{else}}0{{/if}}
}
