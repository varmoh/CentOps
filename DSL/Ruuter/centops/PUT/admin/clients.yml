declaration:
  call: declare
  version: 0.1
  description: "Update client details"
  method: put
  accepts: json
  returns: json
  namespace: centops
  allowlist:
    body:
      - field: name
        type: string
      - field: kubernetesClusterAddress
        type: string
      - field: kubernetesClusterNamespace
        type: string
      - field: argoAppDeploymentName
        type: string
      - field: partOfNetwork
        type: boolean
    params:
      - field: clientId
        type: string

validate_request_param:
  switch:
    - condition: ${incoming.params.clientId == null || incoming.body.name == null || incoming.body.kubernetesClusterAddress == null || incoming.body.kubernetesClusterNamespace == null || incoming.body.argoAppDeploymentName == null || incoming.body.partOfNetwork == null || incoming.headers == null || incoming.headers.cookie == null}
      next: return_bad_request
  next: assign_parameters

assign_parameters:
  assign:
    client_id: ${incoming.params.clientId}
    name: ${incoming.body.name}
    kubernetes_cluster_address: ${incoming.body.kubernetesClusterAddress}
    kubernetes_cluster_namespace: ${incoming.body.kubernetesClusterNamespace}
    argo_app_deployment_name: ${incoming.body.argoAppDeploymentName}
    part_of_network: ${incoming.body.partOfNetwork}
  next: call_create_user_log_template

call_create_user_log_template:
  template: "[#CENTOPS_PROJECT_LAYER]/log-user-request"
  requestType: templates
  body:
    method: "PUT"
    path: "admin/clients"
  headers:
    cookie: ${incoming.headers.cookie}
  result: log_result
  next: send_request

send_request:
  call: http.post
  args:
    url: "[#CENTOPS_RESQL]/update-client"
    body:
      client_id: ${client_id}
      name: ${name}
      kubernetes_cluster_address: ${kubernetes_cluster_address}
      kubernetes_cluster_namespace: ${kubernetes_cluster_namespace}
      argo_app_deployment_name: ${argo_app_deployment_name}
      part_of_network: ${part_of_network}
  result: update_result
  next: return_updated

return_updated:
  status: 204
  return:
  next: end

return_bad_request:
  status: 400
  return: "clientId, name, kubernetesClusterAddress, kubernetesClusterNamespace, argoAppDeploymentName, partOfNetwork and cookie are required"
  next: end
