declaration:
  call: declare
  version: 0.1
  description: "Create a new manifest"
  method: post
  accepts: json
  returns: json
  namespace: centops
  allowlist:
    body:
      - field: clientId
        type: string
        description: "Unique identifier of the client"
      - field: name
        type: string
        description: "Name of the manifest"
      - field: helmVersion
        type: string
        description: "Helm version of a deployment"
      - field: helmValues
        type: string
        description: "Helm values of a deployment"
    headers:
      - field: cookie
        type: string
        description: "Cookie field"

check_for_required_parameters:
  switch:
    - condition: ${incoming.body.clientId == null || incoming.body.helmVersion == null || incoming.body.helmValues == null || incoming.body.name == null || incoming.headers == null || incoming.headers.cookie == null}
      next: return_incorrect_request
  next: extract_request_params

extract_request_params:
  assign:
    client_id: ${incoming.body.clientId}
    name: ${incoming.body.name}
    helm_version: ${incoming.body.helmVersion}
    helm_values: ${incoming.body.helmValues}
  next: call_create_user_log_template

call_create_user_log_template:
  template: "[#CENTOPS_PROJECT_LAYER]/log-user-request"
  requestType: templates
  body:
    method: "POST"
    path: "admin/clients/manifests/create"
  headers:
    cookie: ${incoming.headers.cookie}
  result: create_manifest

create_manifest:
  call: http.post
  args:
    url: "[#CENTOPS_RESQL]/create-manifest"
    body:
      client_id: ${client_id}
      name: ${name}
      helm_version: ${helm_version}
      helm_values: ${helm_values}
  result: results
  next: return_result

return_result:
  wrapper: false
  status: 201
  return: "Manifest created successfully"
  next: end

return_incorrect_request:
  status: 400
  return: "Required parameter are missing: clientId, helmVersion or helmValues or cookie"
  next: end
